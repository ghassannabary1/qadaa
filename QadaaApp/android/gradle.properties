# Project-wide Gradle settings

# IDE (e.g. Android Studio) users:
# Gradle settings configured per-project in build.gradle

# React Native settings
NPM_PACKAGES="react-native@0.81.5"
ANDROID_GRADLE_PLUGIN_VERSION=8.2.2
KOTLIN_VERSION=2.0.21

# Release build settings
RELEASE_STORE_FILE=../qadaa-release-key.jks
RELEASE_KEY_ALIAS=qadaa-release
RELEASE_KEY_PASSWORD=qadaa-release
SIGNING_V1_KEY_PASSWORD=qadaa-signing

# App bundle configuration
ENABLE_APP_BUNDLE=false

# Build version
BUILD_NUMBER=1
BUILD_TYPE=release

# Specifies whether the app is configured to use edge-to-edge via the app config or plugin
# WARNING: This property has been deprecated and will be removed in Expo SDK 55. Use `edgeToEdgeEnabled` or `react.edgeToEdgeEnabled` to determine whether the project is using edge-to-edge.
expo.edgeToEdgeEnabled=true

# Use this property to enable edge-to-edge display support.
# This allows your app to draw behind system bars for an immersive UI.
# Note: Only works with ReactActivity and should not be used with custom Activity.
edgeToEdgeEnabled=true

newArchEnabled=true
hermesEnabled=true
android.useAndroidX=true
android.enableJetifier=true
